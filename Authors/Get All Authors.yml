info:
  name: Get All Authors
  type: http
  seq: 2

http:
  method: GET
  url: "{{blogForge}}/api/v1/authors"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

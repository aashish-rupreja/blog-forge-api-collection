info:
  name: Get All Blogs
  type: http
  seq: 1

http:
  method: GET
  url: "{{blogForge}}/api/v1/blogs"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

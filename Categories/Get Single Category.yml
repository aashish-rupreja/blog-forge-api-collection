info:
  name: Get Single Category
  type: http
  seq: 2

http:
  method: GET
  url: "{{blogForge}}/api/v1/categories/science"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

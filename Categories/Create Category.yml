info:
  name: Create Category
  type: http
  seq: 3

http:
  method: POST
  url: "{{blogForge}}/api/v1/categories"
  body:
    type: json
    data: |-
      {
        "name":"aaaa"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Create Role
  type: http
  seq: 3

http:
  method: POST
  url: "{{blogForge}}/api/v1/roles"
  body:
    type: json
    data: |-
      {
        "name":"ROLE _ ELDER"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

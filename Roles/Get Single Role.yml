info:
  name: Get Single Role
  type: http
  seq: 2

http:
  method: GET
  url: "{{blogForge}}/api/v1/roles/role_user"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

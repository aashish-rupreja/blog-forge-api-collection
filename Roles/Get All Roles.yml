info:
  name: Get All Roles
  type: http
  seq: 1

http:
  method: GET
  url: "{{blogForge}}/api/v1/roles"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

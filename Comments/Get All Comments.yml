info:
  name: Get All Comments
  type: http
  seq: 1

http:
  method: GET
  url: "{{blogForge}}/api/v1/comments"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Get Single User
  type: http
  seq: 2

http:
  method: GET
  url: "{{blogForge}}/api/v1/users/m.hill"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
